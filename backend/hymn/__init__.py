'''__init__.py is what makes Python treat a folder as a package, meaning you can import from it.
Without it, Python won't recognize the hymn folder as a module.
It can be completely empty and still do its job. Later, you can optionally use it to expose a cleaner API, like:
'''